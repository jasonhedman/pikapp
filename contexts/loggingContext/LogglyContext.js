import React from "react";

// simple react context to hold a logging function that can be used
// throughout app
export const LogglyContext = React.createContext(null);

export default LogglyContext;
